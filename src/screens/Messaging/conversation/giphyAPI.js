// giphyAPI.js
export const fetchGiphy = async (query) => {
  // Version simplifiée pour éviter le bug de build
  console.log("Recherche Giphy pour:", query);
  return []; // Retourne un tableau vide pour l'instant
};
